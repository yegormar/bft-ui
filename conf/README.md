# conf/

Non-code configuration for the app or scripts: prompts, rate tables, INI-style config.

- Do not put secrets here; use `.env` (gitignored) for environment-specific values and secrets.
- Document all env vars in the project root `.env.example`.
